const Navbar = (props)=> {
    return (
      <>
      <button className='btn' onClick={}>Beranda</button>
      <button className='btn' onClick={}>Tentang Kami</button>
      <button className='btn' onClick={}>Hubungi Kami</button>
      <button className='btn' onClick={}>Masuk</button>
      </>
    )
};

export default Navbar;